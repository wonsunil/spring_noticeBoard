<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sunil.noticeBoard.mapper.MemberMapper">
    <select id="getAllMember" resultType="sunil.noticeBoard.model.Member">
        SELECT EMAIL, NAME, PHONE, RANK FROM USER_INFO
    </select>
</mapper>